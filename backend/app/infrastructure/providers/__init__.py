"""Vendor-specific `ServerInventoryProvider` implementations.

One subpackage per manager type (`ucs_manager`, `ucs_central`,
`intersight`, `openmanage`, `oneview`, `redfish`) plus `fake`, the
Phase-1 synthetic-data provider.
"""
