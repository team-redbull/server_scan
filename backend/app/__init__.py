"""The FastAPI application.

`main.create_app()` wires domain, application and infrastructure layers
together; `errors`/`exception_handlers` are the RFC 9457 error envelope
shared by every route.
"""
